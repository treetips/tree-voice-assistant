# UI先行実装の方針

- まずUIのみを実装し、動作確認とレビュー指摘の反映を回す。
- Whisper・TTS・通知・音声・更新チェックの実処理は未配線とする。
- 実行ボタン・再生ボタンは `TODO(phase2)` 付きの仮動作（短い待機と状態遷移のみ）とし、
  ローディング表示・活性/非活性・枠色などのUI状態を確認できるようにする。
- 参照元の `RootView.init` の既定引数による `@MainActor` 状態生成は、
  新しい toolchain で誤りになるため、呼び出し側で生成して明示的に渡す形に変える。
